<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.phone.module_resource">

    <application
        android:name=".ResourceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/resource_ic_launcher"
        android:label="@string/resource_app_name"
        android:roundIcon="@mipmap/resource_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/library_AppTheme"
        tools:replace="android:allowBackup, android:label">
        <activity
            android:name=".ui.ResourceActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
    </application>
</manifest>