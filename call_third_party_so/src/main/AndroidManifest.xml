<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.phone.call_third_party_so">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/call_so_ic_launcher"
        android:label="@string/call_so_app_name"
        android:roundIcon="@mipmap/call_so_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/call_so_Theme.RxJava2AndRetrofit2">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>